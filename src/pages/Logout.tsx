import React from "react";
import {
    useHistory
} from 'react-router-dom';

function Logout(){
    const history = useHistory();

    return (
        <>
        </>
    );
}

export default Logout;